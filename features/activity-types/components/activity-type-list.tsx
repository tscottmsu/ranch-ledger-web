import { Activity, Archive, Pencil } from "lucide-react";
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/features/dashboard/components/empty-state";
import { archiveActivityTypeAction } from "../services/activity-type-actions";
import type { ActivityType } from "../types";
export function ActivityTypeList({ activityTypes }: { activityTypes: ActivityType[] }) { if (!activityTypes.length) return <EmptyState icon={Activity} title="No activity types yet" description="Define the activities this ranch offers. Scheduling remains outside this sprint." href="/dashboard/activity-types/new" action="Add activity type" />; return <div className="overflow-hidden rounded-2xl border bg-card"><ul className="divide-y">{activityTypes.map((item) => <li key={item.id} className="flex flex-col justify-between gap-4 p-5 sm:flex-row sm:items-center"><div className="flex items-start gap-4"><span className="grid size-10 shrink-0 place-items-center rounded-xl bg-primary/10 text-primary"><Activity className="size-5" /></span><div><div className="flex items-center gap-2"><h2 className="font-semibold">{item.name}</h2><Badge className={item.active ? "bg-primary/10 text-primary" : ""}>{item.active ? "active" : "archived"}</Badge></div><p className="mt-1 text-sm text-muted-foreground">{item.description || "No description"}</p></div></div><div className="flex justify-end gap-1"><Button asChild variant="ghost" size="sm"><Link href={`/dashboard/activity-types/${item.id}/edit`}><Pencil />Edit</Link></Button>{item.active && <form action={archiveActivityTypeAction.bind(null, item.id)}><Button variant="ghost" size="sm"><Archive />Archive</Button></form>}</div></li>)}</ul></div>; }
