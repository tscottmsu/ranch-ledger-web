import { Archive, PawPrint as HorseIcon, Pencil } from "lucide-react";
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/features/dashboard/components/empty-state";
import { archiveHorseAction } from "../services/horse-actions";
import type { Horse } from "../types";
export function HorseList({ horses }: { horses: Horse[] }) { if (!horses.length) return <EmptyState icon={HorseIcon} title="No horses yet" description="Build the ranch horse roster used by future guided ride planning." href="/dashboard/horses/new" action="Add horse" />; return <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{horses.map((horse) => <div key={horse.id} className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold">{horse.name}</h2><p className="mt-1 text-sm text-muted-foreground">{horse.barn_name || "No barn name"}</p></div><Badge className={horse.status === "active" ? "bg-primary/10 text-primary" : ""}>{horse.status}</Badge></div><dl className="mt-5 grid grid-cols-2 gap-4 text-sm"><div><dt className="text-muted-foreground">Experience</dt><dd className="mt-1 font-medium">{horse.experience_level || "—"}</dd></div><div><dt className="text-muted-foreground">Weight limit</dt><dd className="mt-1 font-medium">{horse.max_rider_weight_lbs ? `${horse.max_rider_weight_lbs} lbs` : "—"}</dd></div></dl><div className="mt-5 flex justify-end gap-1 border-t pt-3"><Button asChild variant="ghost" size="sm"><Link href={`/dashboard/horses/${horse.id}/edit`}><Pencil />Edit</Link></Button>{horse.status === "active" && <form action={archiveHorseAction.bind(null, horse.id)}><Button variant="ghost" size="sm"><Archive />Archive</Button></form>}</div></div>)}</div>; }
