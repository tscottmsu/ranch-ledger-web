import { Archive, Pencil, UserRound } from "lucide-react";
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/features/dashboard/components/empty-state";
import { archiveGuestAction } from "../services/guest-actions";
import type { GuestWithReservation } from "../types";
export function GuestList({ guests }: { guests: GuestWithReservation[] }) { if (!guests.length) return <EmptyState icon={UserRound} title="No guests yet" description="Add individual guest records, with or without a reservation." href="/dashboard/guests/new" action="Add guest" />; return <div className="overflow-hidden rounded-2xl border bg-card"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="border-b bg-muted/40 text-left text-xs uppercase tracking-wide text-muted-foreground"><tr><th className="px-5 py-3">Guest</th><th className="px-5 py-3">Reservation</th><th className="px-5 py-3">Stay</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y">{guests.map((guest) => <tr key={guest.id} className="hover:bg-muted/20"><td className="px-5 py-4 font-medium">{guest.first_name} {guest.last_name}{guest.nickname && <span className="block text-xs font-normal text-muted-foreground">“{guest.nickname}”</span>}</td><td className="px-5 py-4 text-muted-foreground">{guest.reservation?.reservation_name || guest.reservation?.primary_contact_name || "—"}</td><td className="px-5 py-4 text-muted-foreground">{guest.arrival_date || "—"}{guest.departure_date ? ` – ${guest.departure_date}` : ""}</td><td className="px-5 py-4"><Badge className={guest.status === "archived" ? "" : "bg-primary/10 text-primary"}>{guest.status.replaceAll("_", " ")}</Badge></td><td className="px-5 py-4"><div className="flex justify-end gap-1"><Button asChild variant="ghost" size="icon" aria-label="Edit guest"><Link href={`/dashboard/guests/${guest.id}/edit`}><Pencil /></Link></Button>{guest.status !== "archived" && <form action={archiveGuestAction.bind(null, guest.id)}><Button variant="ghost" size="icon" aria-label="Archive guest"><Archive /></Button></form>}</div></td></tr>)}</tbody></table></div></div>; }
