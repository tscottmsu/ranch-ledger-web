import { Archive, Clock, Mountain, Pencil } from "lucide-react";
import Link from "next/link";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { EmptyState } from "@/features/dashboard/components/empty-state";
import { archiveTrailAction } from "../services/trail-actions";
import type { Trail } from "../types";
export function TrailList({ trails }: { trails: Trail[] }) { if (!trails.length) return <EmptyState icon={Mountain} title="No trails yet" description="Add the ranch routes that will support future guided activities." href="/dashboard/trails/new" action="Add trail" />; return <div className="grid gap-4 md:grid-cols-2">{trails.map((trail) => <div key={trail.id} className="rounded-2xl border bg-card p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h2 className="font-semibold">{trail.name}</h2><p className="mt-2 line-clamp-2 text-sm text-muted-foreground">{trail.description || "No description"}</p></div><Badge className={trail.active ? "bg-primary/10 text-primary" : ""}>{trail.active ? "active" : "archived"}</Badge></div><div className="mt-5 flex items-center gap-4 text-sm text-muted-foreground"><span className="capitalize">{trail.difficulty || "Difficulty not set"}</span>{trail.estimated_duration_minutes && <span className="inline-flex items-center gap-1"><Clock className="size-4" />{trail.estimated_duration_minutes} min</span>}</div><div className="mt-5 flex justify-end gap-1 border-t pt-3"><Button asChild variant="ghost" size="sm"><Link href={`/dashboard/trails/${trail.id}/edit`}><Pencil />Edit</Link></Button>{trail.active && <form action={archiveTrailAction.bind(null, trail.id)}><Button variant="ghost" size="sm"><Archive />Archive</Button></form>}</div></div>)}</div>; }
